# 97. Interleaving String
# https://leetcode.com/problems/interleaving-string/
#
# Given strings s1, s2, and s3, find whether s3 is formed by an interleaving
# of s1 and s2. An interleaving of two strings s and t is a configuration
# where s and t are divided into n and m substrings respectively, such that:
# s = s1 + s2 + ... + sn, t = t1 + t2 + ... + tm, |n - m| <= 1
# The interleaving is s1 + t1 + s2 + t2 + ... or t1 + s1 + t2 + s2 + ...
#
# Return true if s3 is formed by an interleaving of s1 and s2, false otherwise.
#
# APPROACH:
# TODO: Describe your approach here
#
# TIME: O(?)
# SPACE: O(?)


class InterleavingString:
    def is_interleave(self, s1: str, s2: str, s3: str) -> bool:
        pass
